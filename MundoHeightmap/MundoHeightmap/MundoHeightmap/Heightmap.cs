using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MundoHeightmap
{
    public class Heightmap
    {
        // 1 - montar um quadrado com index (use o codigo do linebox da aula de colisao para ajudar a entender)
        // 2 - montar um cubo com index (use o codigo do linebox da aula de colisao para ajudar a entender)
        // 3 - montar um grid com index
        // 4 - aplicar o heightmap no grid (esquema com as cores da textura alterando o y dos vertices)
        // 5 - mostrar as normais (apontando pra cima)
        // 6 - recalcular as normais (apontar para a direcao correta)
        // 7 - normalizar normais para nao ficar linhas grandes
    }
}
